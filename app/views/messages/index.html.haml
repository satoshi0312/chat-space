%header
  .header
    = render 'shared/header-group', group: @group

  .account
    = render 'shared/side-header'

.contents
  .group
    .group__name
      = render "shared/side-content"

  .chat__messages
    .chat-main
      = render partial: 'message', collection: @messages

    .form
      = render partial: 'shared/form'

